apiVersion: v1
kind: Service
metadata:
  name: {{ include "example-jvm-svc.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "example-jvm-svc.name" . }}
    helm.sh/chart: {{ include "example-jvm-svc.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "example-jvm-svc.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
